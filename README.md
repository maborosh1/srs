# srs

A **s**tudent **r**ecord **s**ystem project to practice c file handling. It supports CRUD operations and data is stored on a binary file.

To compile run the following command :

```
gcc -o main main.c records/*
```

Run `./main` to start the program.
